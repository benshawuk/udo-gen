<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * Auto-generated ALTER migration from the <%= it.resource %> UDO diff by udo-gen.
 *
 * Only added/dropped columns are emitted automatically. Type and constraint
 * changes are flagged as warnings — fill those in by hand before running.
 */
return new class extends Migration
{
    public function up(): void
    {
<% if (it.warnings.length > 0) { %><% it.warnings.forEach(function (w) { %>        // TODO: <%= w %>
<% }) %>
<% } %>        Schema::table('<%= it.table %>', function (Blueprint $table) {
<% it.addColumns.forEach(function (l) { %>            <%= l %>
<% }) %><% it.dropColumns.forEach(function (l) { %>            <%= l %>
<% }) %>        });
    }

    public function down(): void
    {
        Schema::table('<%= it.table %>', function (Blueprint $table) {
<% it.reverseAddDrops.forEach(function (l) { %>            <%= l %>
<% }) %><% it.reverseDropAdds.forEach(function (l) { %>            <%= l %>
<% }) %>        });
    }
};

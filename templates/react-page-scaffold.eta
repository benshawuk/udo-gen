// Scaffold-once: udo-gen wrote this file once and will not overwrite it.
// Edit freely. The typed metadata lives in `@/udo/<%= it.resource %>`.

import { <%= it.resource %> } from '@/udo';
import { ResourcePage } from '@/lib/udo-ui/resource-page';

export default function <%= it.resource %>Page() {
  return <ResourcePage resource={<%= it.resource %>} />;
}
